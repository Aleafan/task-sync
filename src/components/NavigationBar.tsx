export default function NavigationBar() {
    return (
        <header className="w-full flex items-center sticky top-0 py-2 px-4 shadow-sm bg-background">
            <h1 className="text-lg font-bold">Task Sync</h1>
        </header>
    );
}